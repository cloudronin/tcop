[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tcop-framework"
version = "0.1.0"
description = "Deterministic reference implementation for the Trust Context Project"
requires-python = ">=3.11"
dependencies = ["cryptography>=41"]

[project.scripts]
tcop = "tcop.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
