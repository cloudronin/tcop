# TCOP v0.6 Agent Validation Runtime Amendment 004

## Status

This is an artifact-finalization correction. It does not make a provider
request, regenerate a trace, change a replay, modify a report input, or alter
any TCOP or gateway behavior.

## Trigger

The immutable Amendment 003 live artifact met every recorded completion gate:
the live targets, strict replay matrix, 30 real-gateway trials, authority and
failure invariants, source preservation, and frozen strategy checks. Its
manifest nevertheless recorded `complete: false` because the runner evaluated
the required findings-file existence before creating that file.

## Change

`tcop study agent finalize-live` first verifies the source artifact with its
normal replayable verifier and requires every readiness gate to be true. It
then creates a new content-addressed successor, copies the complete immutable
trace and report set, records the source digest, and changes only completion
metadata to `complete: true`. It fails closed if the source is not valid and
gate-complete.

## Provenance

The final certified artifact is
`artifacts/v0.6-agent-validation-live-final/`. It references the Amendment 003
artifact, which itself retains the complete chain of the scripted parent and
the three earlier incomplete live artifacts.
